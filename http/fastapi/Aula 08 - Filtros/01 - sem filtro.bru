meta {
  name: 01 - Sem filtro
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/produtos/
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.length: eq 5
}

docs {
GET /api/produtos/ — retorna os 5 produtos em memória (array).
}