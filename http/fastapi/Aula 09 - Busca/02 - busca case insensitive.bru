meta {
  name: 02 - search=NOTE (case-insensitive)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/produtos/?search=NOTE
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.length: eq 1
  res.body[0].nome: eq "Notebook"
}

docs {
A busca ignora maiúsculas/minúsculas.
}