meta {
  name: 06 - Remover (restaura base)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/produtos/61/
  body: none
  auth: none
}

assert {
  res.status: eq 204
}

docs {
DELETE remove o id 61, restaurando produtos.json ao estado-base (60 itens).
}