meta {
  name: 03 - Buscar criado (201)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/produtos/6/
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq 6
  res.body.nome: eq "Webcam"
}

docs {
O recurso criado pelo POST fica disponível para GET por id.
}