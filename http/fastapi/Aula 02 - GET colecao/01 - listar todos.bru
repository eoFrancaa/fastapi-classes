meta {
  name: 01 - Listar todos
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/produtos/
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.length: eq 5
}

docs {
GET /api/produtos/ — lista a coleção como array simples (5 produtos em memória).
}